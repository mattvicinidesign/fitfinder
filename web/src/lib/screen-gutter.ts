/** Shared horizontal inset for screen body content and bottom CTAs (16px). */
export const screenGutterX = "px-4";

/** Aligns with IosGroupedSection row titles (section mx-4 + row px-4). */
export const listRowTitleInsetX = "pl-8";

/** Home hero text inset — top matches left (32px). */
export const homeHeroContentInset = "pl-8 pt-8";
