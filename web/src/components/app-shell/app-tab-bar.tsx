"use client";

import { GlassTabBar } from "@/components/app-shell/glass-tab-bar";

/** Primary navigation — floating glass bar with drag magnify. */
export function AppTabBar() {
  return <GlassTabBar />;
}
